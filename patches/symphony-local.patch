diff --git a/elixir/lib/symphony_elixir/config.ex b/elixir/lib/symphony_elixir/config.ex
index 3a9f0d9..facab78 100644
--- a/elixir/lib/symphony_elixir/config.ex
+++ b/elixir/lib/symphony_elixir/config.ex
@@ -8,6 +8,7 @@ defmodule SymphonyElixir.Config do
 
   @default_active_states ["Todo", "In Progress"]
   @default_terminal_states ["Closed", "Cancelled", "Canceled", "Duplicate", "Done"]
+  @default_dispatch_blocking_states []
   @default_linear_endpoint "https://api.linear.app/graphql"
   @default_prompt_template """
   You are working on a Linear issue.
@@ -53,6 +54,7 @@ defmodule SymphonyElixir.Config do
                                  endpoint: [type: :string, default: @default_linear_endpoint],
                                  api_key: [type: {:or, [:string, nil]}, default: nil],
                                  project_slug: [type: {:or, [:string, nil]}, default: nil],
+                                 dispatch_view_id: [type: {:or, [:string, nil]}, default: nil],
                                  assignee: [type: {:or, [:string, nil]}, default: nil],
                                  active_states: [
                                    type: {:list, :string},
@@ -61,6 +63,10 @@ defmodule SymphonyElixir.Config do
                                  terminal_states: [
                                    type: {:list, :string},
                                    default: @default_terminal_states
+                                 ],
+                                 dispatch_blocking_states: [
+                                   type: {:list, :string},
+                                   default: @default_dispatch_blocking_states
                                  ]
                                ]
                              ],
@@ -201,6 +207,11 @@ defmodule SymphonyElixir.Config do
     get_in(validated_workflow_options(), [:tracker, :project_slug])
   end
 
+  @spec linear_dispatch_view_id() :: String.t() | nil
+  def linear_dispatch_view_id do
+    get_in(validated_workflow_options(), [:tracker, :dispatch_view_id])
+  end
+
   @spec linear_assignee() :: String.t() | nil
   def linear_assignee do
     validated_workflow_options()
@@ -219,6 +230,11 @@ defmodule SymphonyElixir.Config do
     get_in(validated_workflow_options(), [:tracker, :terminal_states])
   end
 
+  @spec linear_dispatch_blocking_states() :: [String.t()]
+  def linear_dispatch_blocking_states do
+    get_in(validated_workflow_options(), [:tracker, :dispatch_blocking_states])
+  end
+
   @spec poll_interval_ms() :: pos_integer()
   def poll_interval_ms do
     get_in(validated_workflow_options(), [:polling, :interval_ms])
@@ -463,8 +479,10 @@ defmodule SymphonyElixir.Config do
     |> put_if_present(:endpoint, scalar_string_value(Map.get(section, "endpoint")))
     |> put_if_present(:api_key, binary_value(Map.get(section, "api_key"), allow_empty: true))
     |> put_if_present(:project_slug, scalar_string_value(Map.get(section, "project_slug")))
+    |> put_if_present(:dispatch_view_id, scalar_string_value(Map.get(section, "dispatch_view_id")))
     |> put_if_present(:active_states, csv_value(Map.get(section, "active_states")))
     |> put_if_present(:terminal_states, csv_value(Map.get(section, "terminal_states")))
+    |> put_if_present(:dispatch_blocking_states, csv_value(Map.get(section, "dispatch_blocking_states")))
   end
 
   defp extract_polling_options(section) do
diff --git a/elixir/lib/symphony_elixir/linear/client.ex b/elixir/lib/symphony_elixir/linear/client.ex
index ad8eee5..a855981 100644
--- a/elixir/lib/symphony_elixir/linear/client.ex
+++ b/elixir/lib/symphony_elixir/linear/client.ex
@@ -18,6 +18,8 @@ defmodule SymphonyElixir.Linear.Client do
         title
         description
         priority
+        prioritySortOrder
+        sortOrder
         state {
           name
         }
@@ -54,6 +56,59 @@ defmodule SymphonyElixir.Linear.Client do
   }
   """
 
+  @dispatch_view_query """
+  query SymphonyLinearDispatchViewPoll($viewId: String!, $stateNames: [String!]!, $first: Int!, $relationFirst: Int!, $after: String) {
+    customView(id: $viewId) {
+      viewPreferencesValues {
+        viewOrdering
+        viewOrderingDirection
+      }
+      issues(filter: {state: {name: {in: $stateNames}}}, first: $first, after: $after) {
+        nodes {
+          id
+          identifier
+          title
+          description
+          priority
+          prioritySortOrder
+          sortOrder
+          state {
+            name
+          }
+          branchName
+          url
+          assignee {
+            id
+          }
+          labels {
+            nodes {
+              name
+            }
+          }
+          inverseRelations(first: $relationFirst) {
+            nodes {
+              type
+              issue {
+                id
+                identifier
+                state {
+                  name
+                }
+              }
+            }
+          }
+          createdAt
+          updatedAt
+        }
+        pageInfo {
+          hasNextPage
+          endCursor
+        }
+      }
+    }
+  }
+  """
+
   @query_by_ids """
   query SymphonyLinearIssuesById($ids: [ID!]!, $first: Int!, $relationFirst: Int!) {
     issues(filter: {id: {in: $ids}}, first: $first) {
@@ -63,6 +118,8 @@ defmodule SymphonyElixir.Linear.Client do
         title
         description
         priority
+        prioritySortOrder
+        sortOrder
         state {
           name
         }
@@ -106,6 +163,7 @@ defmodule SymphonyElixir.Linear.Client do
   @spec fetch_candidate_issues() :: {:ok, [Issue.t()]} | {:error, term()}
   def fetch_candidate_issues do
     project_slug = Config.linear_project_slug()
+    dispatch_view_id = normalize_dispatch_view_id(Config.linear_dispatch_view_id())
 
     cond do
       is_nil(Config.linear_api_token()) ->
@@ -116,7 +174,13 @@ defmodule SymphonyElixir.Linear.Client do
 
       true ->
         with {:ok, assignee_filter} <- routing_assignee_filter() do
-          do_fetch_by_states(project_slug, Config.linear_active_states(), assignee_filter)
+          case dispatch_view_id do
+            nil ->
+              do_fetch_by_states(project_slug, Config.linear_active_states(), assignee_filter)
+
+            view_id ->
+              do_fetch_dispatch_view_issues(view_id, Config.linear_active_states(), assignee_filter)
+          end
         end
     end
   end
@@ -218,10 +282,22 @@ defmodule SymphonyElixir.Linear.Client do
     |> finalize_paginated_issues()
   end
 
+  @doc false
+  @spec apply_dispatch_view_order_for_test([Issue.t()], String.t() | nil, String.t() | nil) :: [Issue.t()]
+  def apply_dispatch_view_order_for_test(issues, ordering, direction \\ nil) when is_list(issues) do
+    issues
+    |> sort_dispatch_view_issues(%{ordering: ordering, direction: direction})
+    |> apply_dispatch_order()
+  end
+
   defp do_fetch_by_states(project_slug, state_names, assignee_filter) do
     do_fetch_by_states_page(project_slug, state_names, assignee_filter, nil, [])
   end
 
+  defp do_fetch_dispatch_view_issues(view_id, state_names, assignee_filter) do
+    do_fetch_dispatch_view_page(view_id, state_names, assignee_filter, nil, [], nil)
+  end
+
   defp do_fetch_by_states_page(project_slug, state_names, assignee_filter, after_cursor, acc_issues) do
     with {:ok, body} <-
            graphql(@query, %{
@@ -247,12 +323,143 @@ defmodule SymphonyElixir.Linear.Client do
     end
   end
 
+  defp do_fetch_dispatch_view_page(
+         view_id,
+         state_names,
+         assignee_filter,
+         after_cursor,
+         acc_issues,
+         dispatch_view_ordering
+       ) do
+    with {:ok, body} <-
+           graphql(@dispatch_view_query, %{
+             viewId: view_id,
+             stateNames: state_names,
+             first: @issue_page_size,
+             relationFirst: @issue_page_size,
+             after: after_cursor
+           }),
+         {:ok, issues, page_info, next_dispatch_view_ordering} <-
+           decode_dispatch_view_page_response(body, assignee_filter) do
+      resolved_dispatch_view_ordering = dispatch_view_ordering || next_dispatch_view_ordering
+      updated_acc = prepend_page_issues(issues, acc_issues)
+
+      case next_page_cursor(page_info) do
+        {:ok, next_cursor} ->
+          do_fetch_dispatch_view_page(
+            view_id,
+            state_names,
+            assignee_filter,
+            next_cursor,
+            updated_acc,
+            resolved_dispatch_view_ordering
+          )
+
+        :done ->
+          {:ok,
+           updated_acc
+           |> finalize_paginated_issues()
+           |> sort_dispatch_view_issues(resolved_dispatch_view_ordering)
+           |> apply_dispatch_order()}
+
+        {:error, reason} ->
+          {:error, reason}
+      end
+    end
+  end
+
   defp prepend_page_issues(issues, acc_issues) when is_list(issues) and is_list(acc_issues) do
     Enum.reverse(issues, acc_issues)
   end
 
   defp finalize_paginated_issues(acc_issues) when is_list(acc_issues), do: Enum.reverse(acc_issues)
 
+  defp apply_dispatch_order(issues) when is_list(issues) do
+    issues
+    |> Enum.with_index()
+    |> Enum.map(fn
+      {%Issue{} = issue, dispatch_order} -> %Issue{issue | dispatch_order: dispatch_order}
+      {issue, _dispatch_order} -> issue
+    end)
+  end
+
+  defp sort_dispatch_view_issues(issues, %{ordering: ordering} = dispatch_view_ordering)
+       when is_list(issues) and is_binary(ordering) do
+    case normalize_dispatch_view_ordering(ordering) do
+      "priority" ->
+        Enum.sort_by(issues, &priority_dispatch_sort_tuple/1)
+
+      "manual" ->
+        Enum.sort_by(issues, &manual_dispatch_sort_tuple/1)
+
+      "sortorder" ->
+        Enum.sort_by(issues, &manual_dispatch_sort_tuple/1)
+
+      "createdat" ->
+        Enum.sort_by(issues, &datetime_dispatch_sort_tuple(&1, :created_at, dispatch_view_ordering))
+
+      "updatedat" ->
+        Enum.sort_by(issues, &datetime_dispatch_sort_tuple(&1, :updated_at, dispatch_view_ordering))
+
+      _ ->
+        issues
+    end
+  end
+
+  defp sort_dispatch_view_issues(issues, _dispatch_view_ordering) when is_list(issues), do: issues
+
+  defp priority_dispatch_sort_tuple(%Issue{} = issue) do
+    {
+      priority_sort_order_key(issue.priority_sort_order),
+      manual_sort_order_key(issue.sort_order),
+      created_at_sort_key(issue.created_at),
+      issue.identifier || issue.id || ""
+    }
+  end
+
+  defp manual_dispatch_sort_tuple(%Issue{} = issue) do
+    {
+      manual_sort_order_key(issue.sort_order),
+      created_at_sort_key(issue.created_at),
+      issue.identifier || issue.id || ""
+    }
+  end
+
+  defp datetime_dispatch_sort_tuple(%Issue{} = issue, field, %{direction: direction}) do
+    datetime =
+      case field do
+        :created_at -> issue.created_at
+        :updated_at -> issue.updated_at
+      end
+
+    {
+      datetime_sort_key(datetime, direction),
+      manual_sort_order_key(issue.sort_order),
+      issue.identifier || issue.id || ""
+    }
+  end
+
+  defp priority_sort_order_key(value) when is_integer(value) or is_float(value), do: value
+  defp priority_sort_order_key(_value), do: 9_223_372_036_854_775_807
+
+  defp manual_sort_order_key(value) when is_integer(value) or is_float(value), do: -value
+  defp manual_sort_order_key(_value), do: 9_223_372_036_854_775_807
+
+  defp created_at_sort_key(%DateTime{} = datetime), do: DateTime.to_unix(datetime, :microsecond)
+  defp created_at_sort_key(_datetime), do: 9_223_372_036_854_775_807
+
+  defp datetime_sort_key(%DateTime{} = datetime, direction) do
+    unix = DateTime.to_unix(datetime, :microsecond)
+
+    case normalize_dispatch_view_direction(direction) do
+      "ascending" -> unix
+      "asc" -> unix
+      _ -> -unix
+    end
+  end
+
+  defp datetime_sort_key(_datetime, _direction), do: 9_223_372_036_854_775_807
+
   defp do_fetch_issue_states(ids, assignee_filter) do
     case graphql(@query_by_ids, %{
            ids: ids,
@@ -381,6 +588,32 @@ defmodule SymphonyElixir.Linear.Client do
 
   defp decode_linear_page_response(response, assignee_filter), do: decode_linear_response(response, assignee_filter)
 
+  defp decode_dispatch_view_page_response(
+         %{
+           "data" => %{
+             "customView" => %{
+               "viewPreferencesValues" => view_preferences_values,
+               "issues" => %{
+                 "nodes" => nodes,
+                 "pageInfo" => %{"hasNextPage" => has_next_page, "endCursor" => end_cursor}
+               }
+             }
+           }
+         },
+         assignee_filter
+       ) do
+    with {:ok, issues} <- decode_issue_nodes(nodes, assignee_filter) do
+      {:ok, issues, %{has_next_page: has_next_page == true, end_cursor: end_cursor}, decode_dispatch_view_ordering(view_preferences_values)}
+    end
+  end
+
+  defp decode_dispatch_view_page_response(%{"data" => %{"customView" => nil}}, _assignee_filter) do
+    {:error, :linear_dispatch_view_not_found}
+  end
+
+  defp decode_dispatch_view_page_response(response, assignee_filter),
+    do: decode_linear_page_response(response, assignee_filter)
+
   defp next_page_cursor(%{has_next_page: true, end_cursor: end_cursor})
        when is_binary(end_cursor) and byte_size(end_cursor) > 0 do
     {:ok, end_cursor}
@@ -398,6 +631,8 @@ defmodule SymphonyElixir.Linear.Client do
       title: issue["title"],
       description: issue["description"],
       priority: parse_priority(issue["priority"]),
+      priority_sort_order: parse_sort_order(issue["prioritySortOrder"]),
+      sort_order: parse_sort_order(issue["sortOrder"]),
       state: get_in(issue, ["state", "name"]),
       branch_name: issue["branchName"],
       url: issue["url"],
@@ -412,6 +647,26 @@ defmodule SymphonyElixir.Linear.Client do
 
   defp normalize_issue(_issue, _assignee_filter), do: nil
 
+  defp decode_issue_nodes(nodes, assignee_filter) when is_list(nodes) do
+    issues =
+      nodes
+      |> Enum.map(&normalize_issue(&1, assignee_filter))
+      |> Enum.reject(&is_nil(&1))
+
+    {:ok, issues}
+  end
+
+  defp decode_issue_nodes(_nodes, _assignee_filter), do: {:error, :linear_unknown_payload}
+
+  defp decode_dispatch_view_ordering(%{} = view_preferences_values) do
+    %{
+      ordering: Map.get(view_preferences_values, "viewOrdering"),
+      direction: Map.get(view_preferences_values, "viewOrderingDirection")
+    }
+  end
+
+  defp decode_dispatch_view_ordering(_view_preferences_values), do: nil
+
   defp assignee_field(%{} = assignee, field) when is_binary(field), do: assignee[field]
   defp assignee_field(_assignee, _field), do: nil
 
@@ -527,4 +782,38 @@ defmodule SymphonyElixir.Linear.Client do
 
   defp parse_priority(priority) when is_integer(priority), do: priority
   defp parse_priority(_priority), do: nil
+
+  defp normalize_dispatch_view_id(view_id) when is_binary(view_id) do
+    trimmed = String.trim(view_id)
+    if trimmed == "", do: nil, else: trimmed
+  end
+
+  defp normalize_dispatch_view_id(_view_id), do: nil
+
+  defp normalize_dispatch_view_ordering(ordering) when is_binary(ordering) do
+    ordering
+    |> String.trim()
+    |> String.downcase()
+  end
+
+  defp normalize_dispatch_view_ordering(_ordering), do: nil
+
+  defp normalize_dispatch_view_direction(direction) when is_binary(direction) do
+    direction
+    |> String.trim()
+    |> String.downcase()
+  end
+
+  defp normalize_dispatch_view_direction(_direction), do: nil
+
+  defp parse_sort_order(sort_order) when is_integer(sort_order) or is_float(sort_order), do: sort_order
+
+  defp parse_sort_order(sort_order) when is_binary(sort_order) do
+    case Float.parse(sort_order) do
+      {value, ""} -> value
+      _ -> nil
+    end
+  end
+
+  defp parse_sort_order(_sort_order), do: nil
 end
diff --git a/elixir/lib/symphony_elixir/linear/issue.ex b/elixir/lib/symphony_elixir/linear/issue.ex
index 7ed9476..e8f12b4 100644
--- a/elixir/lib/symphony_elixir/linear/issue.ex
+++ b/elixir/lib/symphony_elixir/linear/issue.ex
@@ -9,6 +9,9 @@ defmodule SymphonyElixir.Linear.Issue do
     :title,
     :description,
     :priority,
+    :dispatch_order,
+    :priority_sort_order,
+    :sort_order,
     :state,
     :branch_name,
     :url,
@@ -26,6 +29,9 @@ defmodule SymphonyElixir.Linear.Issue do
           title: String.t() | nil,
           description: String.t() | nil,
           priority: integer() | nil,
+          dispatch_order: non_neg_integer() | nil,
+          priority_sort_order: number() | nil,
+          sort_order: number() | nil,
           state: String.t() | nil,
           branch_name: String.t() | nil,
           url: String.t() | nil,
diff --git a/elixir/lib/symphony_elixir/orchestrator.ex b/elixir/lib/symphony_elixir/orchestrator.ex
index a4dead1..2a8450c 100644
--- a/elixir/lib/symphony_elixir/orchestrator.ex
+++ b/elixir/lib/symphony_elixir/orchestrator.ex
@@ -273,6 +273,22 @@ defmodule SymphonyElixir.Orchestrator do
     should_dispatch_issue?(issue, state, active_state_set(), terminal_state_set())
   end
 
+  @doc false
+  @spec dispatch_blocked_for_test(([String.t()] -> {:ok, [Issue.t()]} | {:error, term()})) ::
+          boolean()
+  def dispatch_blocked_for_test(fetcher) when is_function(fetcher, 1) do
+    case dispatch_blocking_issues(fetcher) do
+      {:ok, issues} -> issues != []
+      {:error, _reason} -> true
+    end
+  end
+
+  @doc false
+  @spec source_repo_ready_for_dispatch_for_test(String.t() | nil) :: boolean()
+  def source_repo_ready_for_dispatch_for_test(repo_path) do
+    match?(:ready, source_repo_dispatch_status(repo_path))
+  end
+
   @doc false
   @spec revalidate_issue_for_dispatch_for_test(Issue.t(), ([String.t()] -> term())) ::
           {:ok, Issue.t()} | {:skip, Issue.t() | :missing} | {:error, term()}
@@ -303,12 +319,12 @@ defmodule SymphonyElixir.Orchestrator do
       terminal_issue_state?(issue.state, terminal_states) ->
         Logger.info("Issue moved to terminal state: #{issue_context(issue)} state=#{issue.state}; stopping active agent")
 
-        terminate_running_issue(state, issue.id, true)
+        terminate_running_issue(state, issue, true)
 
       !issue_routable_to_worker?(issue) ->
         Logger.info("Issue no longer routed to this worker: #{issue_context(issue)} assignee=#{inspect(issue.assignee_id)}; stopping active agent")
 
-        terminate_running_issue(state, issue.id, false)
+        terminate_running_issue(state, issue, false)
 
       active_issue_state?(issue.state, active_states) ->
         refresh_running_issue_state(state, issue)
@@ -316,7 +332,7 @@ defmodule SymphonyElixir.Orchestrator do
       true ->
         Logger.info("Issue moved to non-active state: #{issue_context(issue)} state=#{issue.state}; stopping active agent")
 
-        terminate_running_issue(state, issue.id, false)
+        terminate_running_issue(state, issue, false)
     end
   end
 
@@ -332,17 +348,22 @@ defmodule SymphonyElixir.Orchestrator do
     end
   end
 
+  defp terminate_running_issue(%State{} = state, %Issue{id: issue_id} = issue, cleanup_workspace) do
+    terminate_running_issue(state, issue_id, cleanup_workspace, issue)
+  end
+
   defp terminate_running_issue(%State{} = state, issue_id, cleanup_workspace) do
+    terminate_running_issue(state, issue_id, cleanup_workspace, nil)
+  end
+
+  defp terminate_running_issue(%State{} = state, issue_id, cleanup_workspace, issue_override) do
     case Map.get(state.running, issue_id) do
       nil ->
         release_issue_claim(state, issue_id)
 
       %{pid: pid, ref: ref, identifier: identifier} = running_entry ->
         state = record_session_completion_totals(state, running_entry)
-
-        if cleanup_workspace do
-          cleanup_issue_workspace(identifier)
-        end
+        hook_issue = issue_override || Map.get(running_entry, :issue)
 
         if is_pid(pid) do
           terminate_task(pid)
@@ -352,6 +373,12 @@ defmodule SymphonyElixir.Orchestrator do
           Process.demonitor(ref, [:flush])
         end
 
+        maybe_run_after_run_hook_for_terminated_issue(hook_issue, identifier)
+
+        if cleanup_workspace do
+          cleanup_issue_workspace(identifier)
+        end
+
         %{
           state
           | running: Map.delete(state.running, issue_id),
@@ -435,31 +462,97 @@ defmodule SymphonyElixir.Orchestrator do
 
   defp terminate_task(_pid), do: :ok
 
+  defp maybe_run_after_run_hook_for_terminated_issue(%Issue{} = issue, identifier) when is_binary(identifier) do
+    workspace = Path.join(Config.workspace_root(), identifier)
+
+    if File.dir?(workspace) do
+      Workspace.run_after_run_hook(workspace, issue)
+    end
+  end
+
+  defp maybe_run_after_run_hook_for_terminated_issue(_issue, _identifier), do: :ok
+
   defp choose_issues(issues, state) do
     active_states = active_state_set()
     terminal_states = terminal_state_set()
 
-    issues
-    |> sort_issues_for_dispatch()
-    |> Enum.reduce(state, fn issue, state_acc ->
-      if should_dispatch_issue?(issue, state_acc, active_states, terminal_states) do
-        dispatch_issue(state_acc, issue)
-      else
-        state_acc
-      end
-    end)
+    case source_repo_dispatch_status(System.get_env("SOURCE_REPO_PATH")) do
+      :ready ->
+        case dispatch_blocking_issues(&Tracker.fetch_issues_by_states/1) do
+          {:ok, []} ->
+            issues
+            |> sort_issues_for_dispatch()
+            |> Enum.reduce(state, fn issue, state_acc ->
+              if should_dispatch_issue?(issue, state_acc, active_states, terminal_states) do
+                dispatch_issue(state_acc, issue)
+              else
+                state_acc
+              end
+            end)
+
+          {:ok, blocking_issues} ->
+            log_dispatch_blocking_issues(blocking_issues)
+            state
+
+          {:error, reason} ->
+            Logger.warning("Skipping new dispatch; failed to fetch blocking issues: #{inspect(reason)}")
+            state
+        end
+
+      {:blocked, reason} ->
+        Logger.info("Skipping new dispatch until SOURCE_REPO_PATH is on fresh main: #{reason}")
+        state
+    end
   end
 
   defp sort_issues_for_dispatch(issues) when is_list(issues) do
     Enum.sort_by(issues, fn
       %Issue{} = issue ->
-        {priority_rank(issue.priority), issue_created_at_sort_key(issue), issue.identifier || issue.id || ""}
+        {
+          dispatch_state_rank(issue.state),
+          issue_dispatch_order_key(issue),
+          issue_sort_order_key(issue),
+          priority_rank(issue.priority),
+          issue_created_at_sort_key(issue),
+          issue.identifier || issue.id || ""
+        }
 
       _ ->
-        {priority_rank(nil), issue_created_at_sort_key(nil), ""}
+        {
+          dispatch_state_rank(nil),
+          issue_dispatch_order_key(nil),
+          issue_sort_order_key(nil),
+          priority_rank(nil),
+          issue_created_at_sort_key(nil),
+          ""
+        }
     end)
   end
 
+  defp dispatch_state_rank(state_name) when is_binary(state_name) do
+    case normalize_issue_state(state_name) do
+      "in progress" -> 0
+      "todo" -> 1
+      _ -> 2
+    end
+  end
+
+  defp dispatch_state_rank(_state_name), do: 2
+
+  defp issue_dispatch_order_key(%Issue{dispatch_order: dispatch_order})
+       when is_integer(dispatch_order) and dispatch_order >= 0,
+       do: dispatch_order
+
+  defp issue_dispatch_order_key(%Issue{}), do: 9_223_372_036_854_775_807
+  defp issue_dispatch_order_key(_issue), do: 9_223_372_036_854_775_807
+
+  defp issue_sort_order_key(%Issue{sort_order: sort_order})
+       when is_integer(sort_order) or is_float(sort_order),
+       do: -sort_order
+
+  defp issue_sort_order_key(%Issue{}), do: 9_223_372_036_854_775_807
+  defp issue_sort_order_key(_issue), do: 9_223_372_036_854_775_807
+
   defp priority_rank(priority) when is_integer(priority) and priority in 1..4, do: priority
   defp priority_rank(_priority), do: 5
 
@@ -575,6 +668,89 @@ defmodule SymphonyElixir.Orchestrator do
     |> MapSet.new()
   end
 
+  defp dispatch_blocking_issues(fetcher) when is_function(fetcher, 1) do
+    case dispatch_blocking_states() do
+      [] -> {:ok, []}
+      states -> fetcher.(states)
+    end
+  end
+
+  defp dispatch_blocking_states do
+    Config.linear_dispatch_blocking_states()
+    |> Enum.map(&to_string/1)
+    |> Enum.map(&String.trim/1)
+    |> Enum.reject(&(&1 == ""))
+    |> Enum.uniq()
+  end
+
+  defp source_repo_dispatch_status(nil), do: {:blocked, "SOURCE_REPO_PATH is not set"}
+  defp source_repo_dispatch_status(""), do: {:blocked, "SOURCE_REPO_PATH is not set"}
+
+  defp source_repo_dispatch_status(repo_path) when is_binary(repo_path) do
+    trimmed_repo_path = String.trim(repo_path)
+
+    cond do
+      trimmed_repo_path == "" ->
+        {:blocked, "SOURCE_REPO_PATH is not set"}
+
+      !File.dir?(trimmed_repo_path) ->
+        {:blocked, "path does not exist: #{trimmed_repo_path}"}
+
+      true ->
+        with {:ok, branch_name} <- git_output(trimmed_repo_path, ["rev-parse", "--abbrev-ref", "HEAD"]),
+             "main" <- String.trim(branch_name),
+             {:ok, head_sha} <- git_output(trimmed_repo_path, ["rev-parse", "HEAD"]),
+             {:ok, origin_main_sha} <- git_output(trimmed_repo_path, ["rev-parse", "origin/main"]) do
+          if String.trim(head_sha) == String.trim(origin_main_sha) do
+            :ready
+          else
+            {:blocked, "HEAD=#{short_sha(head_sha)} does not match origin/main=#{short_sha(origin_main_sha)}"}
+          end
+        else
+          branch_name when is_binary(branch_name) ->
+            {:blocked, "current branch is #{String.trim(branch_name)}"}
+
+          {:error, reason} ->
+            {:blocked, reason}
+        end
+    end
+  end
+
+  defp source_repo_dispatch_status(_repo_path), do: {:blocked, "SOURCE_REPO_PATH is invalid"}
+
+  defp git_output(repo_path, args) when is_binary(repo_path) and is_list(args) do
+    case System.cmd("git", ["-C", repo_path | args], stderr_to_stdout: true) do
+      {output, 0} ->
+        {:ok, String.trim(output)}
+
+      {output, status} ->
+        {:error, "git #{Enum.join(args, " ")} failed with status=#{status}: #{String.trim(output)}"}
+    end
+  end
+
+  defp short_sha(sha) when is_binary(sha) do
+    sha
+    |> String.trim()
+    |> String.slice(0, 12)
+  end
+
+  defp log_dispatch_blocking_issues(blocking_issues) when is_list(blocking_issues) do
+    identifiers =
+      blocking_issues
+      |> Enum.flat_map(fn
+        %Issue{identifier: identifier} when is_binary(identifier) and identifier != "" -> [identifier]
+        _ -> []
+      end)
+      |> Enum.take(5)
+
+    identifier_suffix = if length(blocking_issues) > 5, do: ", ...", else: ""
+
+    Logger.info(
+      "Skipping new dispatch while issues remain in blocking states=#{inspect(dispatch_blocking_states())} " <>
+        "count=#{length(blocking_issues)} identifiers=#{Enum.join(identifiers, ", ")}#{identifier_suffix}"
+    )
+  end
+
   defp dispatch_issue(%State{} = state, issue, attempt \\ nil) do
     case revalidate_issue_for_dispatch(issue, &Tracker.fetch_issue_states_by_ids/1, terminal_state_set()) do
       {:ok, %Issue{} = refreshed_issue} ->
diff --git a/elixir/lib/symphony_elixir/prompt_builder.ex b/elixir/lib/symphony_elixir/prompt_builder.ex
index 1b334a1..bb9d592 100644
--- a/elixir/lib/symphony_elixir/prompt_builder.ex
+++ b/elixir/lib/symphony_elixir/prompt_builder.ex
@@ -22,7 +22,7 @@ defmodule SymphonyElixir.PromptBuilder do
       },
       @render_opts
     )
-    |> IO.iodata_to_binary()
+    |> rendered_prompt_to_binary()
   end
 
   defp prompt_template!({:ok, %{prompt_template: prompt}}), do: default_prompt(prompt)
@@ -61,4 +61,23 @@ defmodule SymphonyElixir.PromptBuilder do
       prompt
     end
   end
+
+  defp rendered_prompt_to_binary(rendered) when is_binary(rendered) do
+    String.replace_invalid(rendered, "")
+  end
+
+  defp rendered_prompt_to_binary(rendered)
+       when is_integer(rendered) and rendered >= 0 and rendered <= 0x10FFFF do
+    <<rendered::utf8>>
+  end
+
+  defp rendered_prompt_to_binary(rendered) when is_list(rendered) do
+    Enum.map_join(rendered, "", &rendered_prompt_to_binary/1)
+  end
+
+  defp rendered_prompt_to_binary(rendered) do
+    rendered
+    |> to_string()
+    |> String.replace_invalid("")
+  end
 end
diff --git a/elixir/test/support/test_support.exs b/elixir/test/support/test_support.exs
index bea30f2..a1a76ac 100644
--- a/elixir/test/support/test_support.exs
+++ b/elixir/test/support/test_support.exs
@@ -96,9 +96,11 @@ defmodule SymphonyElixir.TestSupport do
           tracker_endpoint: "https://api.linear.app/graphql",
           tracker_api_token: "token",
           tracker_project_slug: "project",
+          tracker_dispatch_view_id: nil,
           tracker_assignee: nil,
           tracker_active_states: ["Todo", "In Progress"],
           tracker_terminal_states: ["Closed", "Cancelled", "Canceled", "Duplicate", "Done"],
+          tracker_dispatch_blocking_states: [],
           poll_interval_ms: 30_000,
           workspace_root: Path.join(System.tmp_dir!(), "symphony_workspaces"),
           max_concurrent_agents: 10,
@@ -131,9 +133,11 @@ defmodule SymphonyElixir.TestSupport do
     tracker_endpoint = Keyword.get(config, :tracker_endpoint)
     tracker_api_token = Keyword.get(config, :tracker_api_token)
     tracker_project_slug = Keyword.get(config, :tracker_project_slug)
+    tracker_dispatch_view_id = Keyword.get(config, :tracker_dispatch_view_id)
     tracker_assignee = Keyword.get(config, :tracker_assignee)
     tracker_active_states = Keyword.get(config, :tracker_active_states)
     tracker_terminal_states = Keyword.get(config, :tracker_terminal_states)
+    tracker_dispatch_blocking_states = Keyword.get(config, :tracker_dispatch_blocking_states)
     poll_interval_ms = Keyword.get(config, :poll_interval_ms)
     workspace_root = Keyword.get(config, :workspace_root)
     max_concurrent_agents = Keyword.get(config, :max_concurrent_agents)
@@ -167,9 +171,11 @@ defmodule SymphonyElixir.TestSupport do
         "  endpoint: #{yaml_value(tracker_endpoint)}",
         "  api_key: #{yaml_value(tracker_api_token)}",
         "  project_slug: #{yaml_value(tracker_project_slug)}",
+        "  dispatch_view_id: #{yaml_value(tracker_dispatch_view_id)}",
         "  assignee: #{yaml_value(tracker_assignee)}",
         "  active_states: #{yaml_value(tracker_active_states)}",
         "  terminal_states: #{yaml_value(tracker_terminal_states)}",
+        "  dispatch_blocking_states: #{yaml_value(tracker_dispatch_blocking_states)}",
         "polling:",
         "  interval_ms: #{yaml_value(poll_interval_ms)}",
         "workspace:",
diff --git a/elixir/test/symphony_elixir/core_test.exs b/elixir/test/symphony_elixir/core_test.exs
index 400c006..c63292f 100644
--- a/elixir/test/symphony_elixir/core_test.exs
+++ b/elixir/test/symphony_elixir/core_test.exs
@@ -5,15 +5,19 @@ defmodule SymphonyElixir.CoreTest do
     write_workflow_file!(Workflow.workflow_file_path(),
       tracker_api_token: nil,
       tracker_project_slug: nil,
+      tracker_dispatch_view_id: nil,
       poll_interval_ms: nil,
       tracker_active_states: nil,
       tracker_terminal_states: nil,
+      tracker_dispatch_blocking_states: nil,
       codex_command: nil
     )
 
     assert Config.poll_interval_ms() == 30_000
     assert Config.linear_active_states() == ["Todo", "In Progress"]
     assert Config.linear_terminal_states() == ["Closed", "Cancelled", "Canceled", "Duplicate", "Done"]
+    assert Config.linear_dispatch_blocking_states() == []
+    assert Config.linear_dispatch_view_id() == nil
     assert Config.linear_assignee() == nil
     assert Config.agent_max_turns() == 20
 
@@ -32,6 +36,12 @@ defmodule SymphonyElixir.CoreTest do
     write_workflow_file!(Workflow.workflow_file_path(), tracker_active_states: "Todo,  Review,")
     assert Config.linear_active_states() == ["Todo", "Review"]
 
+    write_workflow_file!(Workflow.workflow_file_path(), tracker_dispatch_blocking_states: "In Review, QA")
+    assert Config.linear_dispatch_blocking_states() == ["In Review", "QA"]
+
+    write_workflow_file!(Workflow.workflow_file_path(), tracker_dispatch_view_id: "view-123")
+    assert Config.linear_dispatch_view_id() == "view-123"
+
     write_workflow_file!(Workflow.workflow_file_path(),
       tracker_api_token: "token",
       tracker_project_slug: nil
@@ -73,8 +83,9 @@ defmodule SymphonyElixir.CoreTest do
 
   test "current WORKFLOW.md file is valid and complete" do
     original_workflow_path = Workflow.workflow_file_path()
+    repo_workflow_path = Path.expand("../../../../../WORKFLOW.md", __DIR__)
     on_exit(fn -> Workflow.set_workflow_file_path(original_workflow_path) end)
-    Workflow.clear_workflow_file_path()
+    Workflow.set_workflow_file_path(repo_workflow_path)
 
     assert {:ok, %{config: config, prompt: prompt}} = Workflow.load()
     assert is_map(config)
@@ -83,15 +94,16 @@ defmodule SymphonyElixir.CoreTest do
     assert is_map(tracker)
     assert Map.get(tracker, "kind") == "linear"
     assert is_binary(Map.get(tracker, "project_slug"))
+    assert Map.get(tracker, "dispatch_view_id") == "07c31365-7dc2-4bcc-a46b-4bc527b800ee"
     assert is_list(Map.get(tracker, "active_states"))
     assert is_list(Map.get(tracker, "terminal_states"))
+    assert Map.get(tracker, "dispatch_blocking_states") == ["In Review"]
 
     hooks = Map.get(config, "hooks", %{})
     assert is_map(hooks)
-    assert Map.get(hooks, "after_create") =~ "git clone --depth 1 https://github.com/openai/symphony ."
-    assert Map.get(hooks, "after_create") =~ "cd elixir && mise trust"
-    assert Map.get(hooks, "after_create") =~ "mise exec -- mix deps.get"
-    assert Map.get(hooks, "before_remove") =~ "cd elixir && mise exec -- mix workspace.before_remove"
+    assert Map.get(hooks, "after_create") =~ "git clone --origin origin --branch main --single-branch"
+    assert Map.get(hooks, "after_run") =~ "scripts/symphony-after-run.sh"
+    assert is_nil(Map.get(hooks, "before_remove"))
 
     assert String.trim(prompt) != ""
     assert is_binary(Config.workflow_prompt())
@@ -217,12 +229,14 @@ defmodule SymphonyElixir.CoreTest do
     issue_id = "issue-1"
     issue_identifier = "MT-555"
     workspace = Path.join(test_root, issue_identifier)
+    after_run_marker = Path.join(test_root, "after_run.log")
 
     try do
       write_workflow_file!(Workflow.workflow_file_path(),
         workspace_root: test_root,
         tracker_active_states: ["Todo", "In Progress", "In Review"],
-        tracker_terminal_states: ["Closed", "Cancelled", "Canceled", "Duplicate"]
+        tracker_terminal_states: ["Closed", "Cancelled", "Canceled", "Duplicate"],
+        hook_after_run: "echo after_run > \"#{after_run_marker}\""
       )
 
       File.mkdir_p!(test_root)
@@ -265,6 +279,7 @@ defmodule SymphonyElixir.CoreTest do
       refute MapSet.member?(updated_state.claimed, issue_id)
       refute Process.alive?(agent_pid)
       assert File.exists?(workspace)
+      assert File.read!(after_run_marker) == "after_run\n"
     after
       File.rm_rf(test_root)
     end
@@ -496,7 +511,7 @@ defmodule SymphonyElixir.CoreTest do
     assert %{attempt: 3, due_at_ms: due_at_ms, identifier: "MT-559", error: "agent exited: :boom"} =
              state.retry_attempts[issue_id]
 
-    assert_due_in_range(due_at_ms, 39_500, 40_500)
+    assert_due_in_range(due_at_ms, 39_000, 40_500)
   end
 
   test "first abnormal worker exit waits before retrying" do
@@ -597,6 +612,37 @@ defmodule SymphonyElixir.CoreTest do
     assert prompt =~ "updated=2026-02-26T18:07:03Z"
   end
 
+  test "prompt builder preserves unicode workflow content as valid utf-8" do
+    workflow_prompt = "Привет, {{ issue.title }}. Описание: {{ issue.description }}"
+
+    write_workflow_file!(Workflow.workflow_file_path(), prompt: workflow_prompt)
+
+    issue = %Issue{
+      identifier: "MT-702",
+      title: "Проверка Unicode",
+      description: "Русский текст не должен ломать JSON",
+      state: "Todo",
+      url: "https://example.org/issues/MT-702",
+      labels: []
+    }
+
+    prompt = PromptBuilder.build_prompt(issue)
+
+    assert String.valid?(prompt)
+
+    encoded =
+      Jason.encode!(%{
+        "params" => %{
+          "input" => [
+            %{"type" => "text", "text" => prompt}
+          ]
+        }
+      })
+
+    assert String.valid?(encoded)
+    assert encoded =~ "Проверка Unicode"
+  end
+
   test "prompt builder normalizes nested date-like values, maps, and structs in issue fields" do
     write_workflow_file!(Workflow.workflow_file_path(), prompt: "Ticket {{ issue.identifier }}")
 
diff --git a/elixir/test/symphony_elixir/workspace_and_config_test.exs b/elixir/test/symphony_elixir/workspace_and_config_test.exs
index 10f9f52..54ff4b4 100644
--- a/elixir/test/symphony_elixir/workspace_and_config_test.exs
+++ b/elixir/test/symphony_elixir/workspace_and_config_test.exs
@@ -276,6 +276,7 @@ defmodule SymphonyElixir.WorkspaceAndConfigTest do
       "title" => "Blocked todo",
       "description" => "Needs dependency",
       "priority" => 2,
+      "sortOrder" => -42.5,
       "state" => %{"name" => "Todo"},
       "branchName" => "mt-1",
       "url" => "https://example.org/issues/MT-1",
@@ -312,6 +313,7 @@ defmodule SymphonyElixir.WorkspaceAndConfigTest do
     assert issue.blocked_by == [%{id: "issue-2", identifier: "MT-2", state: "In Progress"}]
     assert issue.labels == ["backend"]
     assert issue.priority == 2
+    assert issue.sort_order == -42.5
     assert issue.state == "Todo"
     assert issue.assignee_id == "user-1"
     assert issue.assigned_to_worker
@@ -377,42 +379,155 @@ defmodule SymphonyElixir.WorkspaceAndConfigTest do
     assert log =~ "Variable \\\"$ids\\\" got invalid value"
   end
 
-  test "orchestrator sorts dispatch by priority then oldest created_at" do
-    issue_same_priority_older = %Issue{
-      id: "issue-old-high",
+  test "orchestrator resumes in-progress issues before todo issues" do
+    resumed_issue = %Issue{
+      id: "issue-in-progress",
       identifier: "MT-200",
-      title: "Old high priority",
+      title: "Resume active task",
+      state: "In Progress",
+      sort_order: -10,
+      priority: 4,
+      created_at: ~U[2026-01-03 00:00:00Z]
+    }
+
+    todo_issue = %Issue{
+      id: "issue-todo",
+      identifier: "MT-201",
+      title: "Queued task",
       state: "Todo",
+      sort_order: -100,
       priority: 1,
       created_at: ~U[2026-01-01 00:00:00Z]
     }
 
-    issue_same_priority_newer = %Issue{
-      id: "issue-new-high",
-      identifier: "MT-201",
-      title: "New high priority",
+    sorted =
+      Orchestrator.sort_issues_for_dispatch_for_test([
+        todo_issue,
+        resumed_issue
+      ])
+
+    assert Enum.map(sorted, & &1.identifier) == ["MT-200", "MT-201"]
+  end
+
+  test "orchestrator sorts todo dispatch by Linear manual sort order before priority" do
+    issue_top = %Issue{
+      id: "issue-top",
+      identifier: "MT-300",
+      title: "Top of column",
+      state: "Todo",
+      sort_order: -100,
+      priority: 4,
+      created_at: ~U[2026-01-03 00:00:00Z]
+    }
+
+    issue_bottom = %Issue{
+      id: "issue-bottom",
+      identifier: "MT-301",
+      title: "Lower in column",
+      state: "Todo",
+      sort_order: -200,
+      priority: 1,
+      created_at: ~U[2026-01-01 00:00:00Z]
+    }
+
+    sorted =
+      Orchestrator.sort_issues_for_dispatch_for_test([
+        issue_bottom,
+        issue_top
+      ])
+
+    assert Enum.map(sorted, & &1.identifier) == ["MT-300", "MT-301"]
+  end
+
+  test "orchestrator preserves dispatch view order before manual sort order" do
+    issue_top = %Issue{
+      id: "issue-dispatch-top",
+      identifier: "MT-320",
+      title: "Top in dispatch view",
+      state: "Todo",
+      dispatch_order: 0,
+      sort_order: -200,
+      priority: 4,
+      created_at: ~U[2026-01-03 00:00:00Z]
+    }
+
+    issue_bottom = %Issue{
+      id: "issue-dispatch-bottom",
+      identifier: "MT-321",
+      title: "Lower in dispatch view",
       state: "Todo",
+      dispatch_order: 1,
+      sort_order: -100,
       priority: 1,
-      created_at: ~U[2026-01-02 00:00:00Z]
+      created_at: ~U[2026-01-01 00:00:00Z]
     }
 
-    issue_lower_priority_older = %Issue{
-      id: "issue-old-low",
-      identifier: "MT-199",
-      title: "Old lower priority",
+    sorted =
+      Orchestrator.sort_issues_for_dispatch_for_test([
+        issue_bottom,
+        issue_top
+      ])
+
+    assert Enum.map(sorted, & &1.identifier) == ["MT-320", "MT-321"]
+  end
+
+  test "dispatch view priority ordering derives dispatch order from priority sort order" do
+    issue_first = %Issue{
+      id: "issue-priority-first",
+      identifier: "MT-330",
+      title: "First by board priority order",
+      state: "Todo",
+      priority_sort_order: -200,
+      sort_order: -100,
+      created_at: ~U[2026-01-03 00:00:00Z]
+    }
+
+    issue_second = %Issue{
+      id: "issue-priority-second",
+      identifier: "MT-331",
+      title: "Second by board priority order",
       state: "Todo",
-      priority: 2,
-      created_at: ~U[2025-12-01 00:00:00Z]
+      priority_sort_order: -150,
+      sort_order: -200,
+      created_at: ~U[2026-01-01 00:00:00Z]
+    }
+
+    ordered =
+      Client.apply_dispatch_view_order_for_test(
+        [issue_second, issue_first],
+        "priority"
+      )
+
+    assert Enum.map(ordered, &{&1.identifier, &1.dispatch_order}) == [{"MT-330", 0}, {"MT-331", 1}]
+  end
+
+  test "issues missing manual sort order fall back behind ordered issues" do
+    ordered_issue = %Issue{
+      id: "issue-ordered",
+      identifier: "MT-400",
+      title: "Ordered task",
+      state: "Todo",
+      sort_order: -50,
+      priority: 4,
+      created_at: ~U[2026-01-03 00:00:00Z]
+    }
+
+    missing_issue = %Issue{
+      id: "issue-missing",
+      identifier: "MT-401",
+      title: "Missing sort order",
+      state: "Todo",
+      priority: 1,
+      created_at: ~U[2026-01-01 00:00:00Z]
     }
 
     sorted =
       Orchestrator.sort_issues_for_dispatch_for_test([
-        issue_lower_priority_older,
-        issue_same_priority_newer,
-        issue_same_priority_older
+        missing_issue,
+        ordered_issue
       ])
 
-    assert Enum.map(sorted, & &1.identifier) == ["MT-200", "MT-201", "MT-199"]
+    assert Enum.map(sorted, & &1.identifier) == ["MT-400", "MT-401"]
   end
 
   test "todo issue with non-terminal blocker is not dispatch-eligible" do
@@ -477,6 +592,118 @@ defmodule SymphonyElixir.WorkspaceAndConfigTest do
     assert Orchestrator.should_dispatch_issue_for_test(issue, state)
   end
 
+  test "new dispatches are globally blocked while review issues exist" do
+    write_workflow_file!(Workflow.workflow_file_path(),
+      tracker_dispatch_blocking_states: ["In Review"]
+    )
+
+    fetcher = fn ["In Review"] ->
+      {:ok,
+       [
+         %Issue{
+           id: "review-1",
+           identifier: "MT-1008",
+           title: "Awaiting review",
+           state: "In Review"
+         }
+       ]}
+    end
+
+    assert Orchestrator.dispatch_blocked_for_test(fetcher)
+  end
+
+  test "new dispatches proceed when blocking review column is empty" do
+    write_workflow_file!(Workflow.workflow_file_path(),
+      tracker_dispatch_blocking_states: ["In Review"]
+    )
+
+    fetcher = fn ["In Review"] -> {:ok, []} end
+
+    refute Orchestrator.dispatch_blocked_for_test(fetcher)
+  end
+
+  test "source repo freshness gate requires local main to match origin/main" do
+    test_root =
+      Path.join(
+        System.tmp_dir!(),
+        "symphony-elixir-source-repo-fresh-#{System.unique_integer([:positive])}"
+      )
+
+    try do
+      repo_path = Path.join(test_root, "repo")
+      File.mkdir_p!(repo_path)
+
+      assert {_, 0} = System.cmd("git", ["init", "-b", "main"], cd: repo_path)
+      File.write!(Path.join(repo_path, "README.md"), "v1\n")
+      assert {"", 0} = System.cmd("git", ["add", "README.md"], cd: repo_path)
+
+      assert {_, 0} =
+               System.cmd(
+                 "git",
+                 ["-c", "user.name=Test", "-c", "user.email=test@example.com", "commit", "-m", "init"],
+                 cd: repo_path
+               )
+
+      {initial_head, 0} = System.cmd("git", ["rev-parse", "HEAD"], cd: repo_path)
+      initial_head = String.trim(initial_head)
+
+      assert {"", 0} =
+               System.cmd("git", ["update-ref", "refs/remotes/origin/main", initial_head], cd: repo_path)
+
+      assert Orchestrator.source_repo_ready_for_dispatch_for_test(repo_path)
+
+      File.write!(Path.join(repo_path, "README.md"), "v2\n")
+      assert {"", 0} = System.cmd("git", ["add", "README.md"], cd: repo_path)
+
+      assert {_, 0} =
+               System.cmd(
+                 "git",
+                 ["-c", "user.name=Test", "-c", "user.email=test@example.com", "commit", "-m", "stale"],
+                 cd: repo_path
+               )
+
+      refute Orchestrator.source_repo_ready_for_dispatch_for_test(repo_path)
+    after
+      File.rm_rf(test_root)
+    end
+  end
+
+  test "source repo freshness gate blocks when local repo is not on main" do
+    test_root =
+      Path.join(
+        System.tmp_dir!(),
+        "symphony-elixir-source-repo-branch-#{System.unique_integer([:positive])}"
+      )
+
+    try do
+      repo_path = Path.join(test_root, "repo")
+      File.mkdir_p!(repo_path)
+
+      assert {_, 0} = System.cmd("git", ["init", "-b", "main"], cd: repo_path)
+      File.write!(Path.join(repo_path, "README.md"), "v1\n")
+      assert {"", 0} = System.cmd("git", ["add", "README.md"], cd: repo_path)
+
+      assert {_, 0} =
+               System.cmd(
+                 "git",
+                 ["-c", "user.name=Test", "-c", "user.email=test@example.com", "commit", "-m", "init"],
+                 cd: repo_path
+               )
+
+      {head_sha, 0} = System.cmd("git", ["rev-parse", "HEAD"], cd: repo_path)
+      head_sha = String.trim(head_sha)
+
+      assert {"", 0} =
+               System.cmd("git", ["update-ref", "refs/remotes/origin/main", head_sha], cd: repo_path)
+
+      assert {"", 0} = System.cmd("git", ["switch", "-c", "codex/test-branch"], cd: repo_path)
+
+      refute Orchestrator.source_repo_ready_for_dispatch_for_test(repo_path)
+    after
+      File.rm_rf(test_root)
+    end
+  end
+
   test "dispatch revalidation skips stale todo issue once a non-terminal blocker appears" do
     stale_issue = %Issue{
       id: "blocked-2",
@@ -720,6 +947,7 @@ defmodule SymphonyElixir.WorkspaceAndConfigTest do
     write_workflow_file!(Workflow.workflow_file_path(),
       tracker_active_states: %{todo: true},
       tracker_terminal_states: %{done: true},
+      tracker_dispatch_blocking_states: %{review: true},
       poll_interval_ms: %{bad: true},
       workspace_root: 123,
       max_retry_backoff_ms: 0,
@@ -734,6 +962,7 @@ defmodule SymphonyElixir.WorkspaceAndConfigTest do
 
     assert Config.linear_active_states() == ["Todo", "In Progress"]
     assert Config.linear_terminal_states() == ["Closed", "Cancelled", "Canceled", "Duplicate", "Done"]
+    assert Config.linear_dispatch_blocking_states() == []
     assert Config.poll_interval_ms() == 30_000
     assert Config.workspace_root() == Path.join(System.tmp_dir!(), "symphony_workspaces")
     assert Config.max_retry_backoff_ms() == 300_000

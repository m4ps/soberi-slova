{
  "name": "endless-word-grid",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:proxy": "sdk-dev-proxy --host localhost:5173 --port 8080 --dev-mode=true",
    "dev:proxy:prod": "sdk-dev-proxy --host localhost:4173 --port 8080 --dev-mode=false",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pixi.js": "^8.16.0"
  },
  "devDependencies": {
    "@yandex-games/sdk-dev-proxy": "^0.0.2",
    "@types/node": "^24.10.13",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
